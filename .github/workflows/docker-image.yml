name: Docker Image CI

on:
  push:
    branches: [ "minishell" ]
  pull_request:
    branches: [ "minishell" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: cd .devcontainer && docker build . --file Dockerfile --tag my-image-name:$(date +%s)
